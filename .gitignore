node_modules
dist
# Keep environment variables out of version control
.env
.env.test

/src/generated/prisma

# Configuração local/pessoal do Claude Code — não é compartilhada no repo
.claude/settings.local.json
